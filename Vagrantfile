#This is the vagrant file used to build the lab server for the turnserver environment. Basically, this is the foundation of the entire infrastructure that is Jitsi
#In a way, the version of Ubuntu used is less important than the stability of the box being tested. I picked this because the "bento" developer typically releases reliable boxes. 

Vagrant.configure("2") do |config|
  config.vm.box = "bento/ubuntu-24.04"
  config.vm.box_version = "202502.21.0"
end
