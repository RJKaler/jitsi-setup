#source: https://github.com/jitsi/jibri



Note: the following components are optional but required for Jibri as an enhancement to standard features 

#ffmpeg with x11 support 

#ffmpeg install 
echo "Attempting to install ffmpeg..." 

if sudo apt-get install ffmpeg -y; then 
    echo "Successfully installed ffmpeg" 
else 
    error 
fi

